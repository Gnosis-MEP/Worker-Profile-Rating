from setuptools import setup

setup(
    name='worker_profile_rating',
    version='0.1',
    description='Service responsible for rating (fuzzy or crisp) values of each worker characteristic based on all the workers in the system',
    author='Felipe Arruda Pontes',
    author_email='felipe.arruda.pontes@insight-centre.org',
    packages=['worker_profile_rating'],
    zip_safe=False
)
